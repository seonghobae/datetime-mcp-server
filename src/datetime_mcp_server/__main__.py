"""
Entry point for running the datetime MCP server as a module.

This allows running the server with:
    python -m datetime_mcp_server
"""

from .main import main

if __name__ == "__main__":
    main()
